(ns workshop.game.game_v2-test
  (:require [clojure.test :refer :all])
  (:require [workshop.game.game_v2 :as g]))

(def state
  {
   :secret 1
   :guess 2
   :status :start
   }
  )

(deftest test-run-game
  (is )
  )
